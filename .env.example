# Database (required). Use Railway Postgres connection URL in production.
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# NextAuth (required). Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-at-least-32-chars"

# App URL. Required for local dev. In Vercel production leave UNSET so the request host is used (fixes login when using custom domain or multiple URLs).
NEXTAUTH_URL="http://localhost:3000"

# Email (optional). Without these, tracking/notification emails are skipped or logged.
# Get API key at https://resend.com. Verify domain for production.
# RESEND_API_KEY="re_xxxx"
# RESEND_FROM_ADDRESS="LoanFlow <notifications@yourdomain.com>"

# Optional: public URL for links in emails (avoids localhost in emails, better deliverability).
# EMAIL_APP_URL="https://your-app.vercel.app"
